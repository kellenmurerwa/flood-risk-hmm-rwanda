# Runtime dependencies for the DEPLOYED Streamlit dashboard (dashboard.py).
# Pinned to the exact versions validated end-to-end (clean venv install +
# headless boot returning HTTP 200) so cloud deployment is deterministic.
# xgboost is pinned to 1.7.5 to match the version the model was pickled with.
#
# For the full data-build / training / analysis pipeline (`python main.py all`)
# install requirements-training.txt instead (adds shap, hmmlearn, scipy, etc.).
streamlit==1.51.0
pandas==2.3.3
numpy==2.2.6
pyarrow==21.0.0
scikit-learn==1.7.1     # provides the estimator classes the pickled model unpickles into
xgboost==1.7.5          # MUST match the version model_outputs_real/xgboost_model.joblib was saved with
pydeck==0.9.1
matplotlib==3.10.7
